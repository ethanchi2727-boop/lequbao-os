# Project state — V6.1 rebuild

## Active branch

`main`

The V5 rollback baseline remains preserved in Git history at `f70b4674c82f99db8424bba8854aeb8a7a11d362`. Remote `main` contains the merged V6.1 candidate application at merge commit `bb59cad44747ad3c1ecb607bf0f2de51937ab058`; production activation remains gated separately.

## Current objective

Rebuild the repository around the V6.1 `乐趣宝 + 乐趣生活` baseline. Preserve verified business behavior and migration evidence, but do not preserve the V5 application topology, naming, SQLite persistence, or UI architecture merely for compatibility.

The active frontend objective is the 50-stage productization rebuild in `docs/release/50_STAGE_FRONTEND_PRODUCTIZATION_PLAN.md`. The prior statement that all 197 leaves are `authoritative` records contract/API coverage only; it is not evidence that 197 dedicated interfaces are visually designed, interaction-complete or browser/device accepted. The page-level truth is now `docs/release/frontend-page-completion.json`.

ADR-0012 corrects the active terminal topology: `apps/life-uniapp` is the shared Vue 3 UniApp source for 乐趣生活 H5 and WeChat; 乐趣宝 remains PC-first in `apps/workbench-web`, while `apps/bao-uniapp` supplies its employee mobile H5 and WeChat package. The merchant independent mini-program remains a separate consumer storefront template. Both new UniApp packages now pass structure tests and real H5 plus `mp-weixin` production builds. 乐趣生活 now has device-bound WeChat and mobile-OTP identity exchange, rotating refresh credentials, server-side revocation, linked-merchant store/product discovery, live cart add/read/remove, checkout quote/order creation and order history. 乐趣宝 mobile now uses the revocable employee session audience and authoritative today, merchant/store, order/refund, customer-service queue/task and current-context reads instead of hard-coded operating numbers. Its first bounded write workflows are also live: employees can atomically claim a queued human-service conversation and can close an open/assigned service task only after explicit confirmation, with permission/store checks, idempotency, optimistic versioning and audit enforced by the API.

The Web deployment artifact now carries three isolated static roots: 乐趣宝 PC (`/bao/`), 乐趣生活 UniApp H5 (`/life/`) and 乐趣宝 UniApp mobile H5 (`/bao-mobile/`). The production and preview servers route each prefix to its own SPA root and health-check the complete production bundle. Both UniApp shells now include five task-oriented first-pass screens and explicit development-data labelling. Preview data is available only when both build-time preview flags and the development mock gateway are enabled; production builds fail closed without real identity assertions. Browser/device acceptance, official mobile-OTP/enterprise-WeCom provider configuration, payment component activation and real WeChat AppID integration remain unfinished.

After the consumer identity, first live Life commerce slice and bounded Bao mobile operations, the complete local `pnpm check` is green: formatting, ESLint, contracts/RBAC/OpenAPI/security/operations/deployment gates, all type checks, 153 test files/899 tests, 201 implemented OpenAPI paths, 乐趣宝 PC build, both UniApp H5 and both UniApp `mp-weixin` builds, API/Worker builds and the five-root production artifact policy all passed at commit `44d283dfca1bedc0f9cf1d0b20cfb1fbaddc206a`. The current artifact manifest is `b0d5e254f8070aba`; the security gate scanned 690 text files, rebuilt and exactly matched all 760 SBOM components, and audited 364 production package versions. Supply-chain gates discover all formal workspace manifests and every workflow YAML, structurally validate workflow container references, reject unsafe local Action paths, fail closed on incomplete GitHub check/Artifact listings and bind downloaded candidate/release artifacts to GitHub SHA-256 metadata. Production configuration and controlled preflight share fail-closed rules for exact names/storage classes, placeholders, surrounding whitespace, canonical address keys, embedded gateway credentials, equivalent local/unspecified hosts, blanket trusted-proxy ranges and relative performance-report paths. Controlled evidence now uses shared credential/contact-data redaction, canonical UTC chronology, exact context/decision schemas, opaque governance identities, plan-source binding at every lifecycle entry, single-file capture paths, physically resolved package output parents and pre-write result assembly validation. Capture receipts bind the complete execution-context hash, every operational timestamp must fall between workspace creation and evidence capture, and capture receipts must fall inside the independently reviewed suite execution window. Disaster-recovery evidence additionally hashes database and tenant references, emits the exact complete financial metric set per tenant, binds deletion replay to the restored database, enforces exact manifest types, canonical millisecond UTC, non-overwriting publication, non-production targeting, complete current SQL fixture coverage, recomputed RPO/RTO, structured cross-domain WAL recovery and per-target deletion receipts; performance evidence redacts routing and database names, binds canonical targets, exact 27-version migration inventories, same-database snapshots and a bounded monitoring window to exact candidate packages and one protected publisher run while independently reconciling acknowledgements and metrics. V5 inventory evidence excludes raw table names, rejects unsafe numeric counts and exposes only reviewed hashes and aggregate counts. WeChat, legal, greenfield, intake, payment, PostgreSQL replay, commerce and plugin evidence now cross-reconcile device/build versions, approval receipts, freshly inspected inventory coverage, object provenance, order references, pooled sessions, ordered inventory execution, ledger rows and exact network-log bytes. Identity/privacy/on-call evidence recomputes session, export, deletion and retention timelines, proves revocation while a sampled session is active, proves pre-rotation Secret denial, binds export to a sampled live session, and proves delivery/acknowledgement for all 14 frozen alert codes. Migration `0027` keeps the audited table count at 164 while adding assertion replay protection, immutable device/session evidence, refresh rotation and revocation. Existing preview volumes receive this expand-only migration through a one-shot Compose dependency before the app starts. This workstation has no `psql`, `bash` or Docker executable, so the new PostgreSQL fixture, migration job and container stack are CI/server-wired but not claimed as locally executed.

The Life productization slice now covers all 38 frozen leaves with independent UniApp routes. Platform-consumer flows include discovery/map, products, cart/checkout, orders, payment initiation, verification credentials, refunds/aftercare, rewards, encrypted addresses and encrypted invoice profiles. PAGE-254/255/258/262/264 require a merchant-scoped consumer identity that the Life session cannot impersonate, so they render an explicit boundary and do not call merchant customer-service/profile APIs. All 38 remain `accepted: false` until real sessions, provider sandboxes, browser coverage and WeChat-device evidence are attached.

The first ten dedicated Life leaves are now implemented in the shared UniApp source: PAGE-198/200/201/203/204 cover city, taxonomy, category results and search; PAGE-207/209/210/211/213 cover selected goods, product detail, variant selection, verified trace reports and the live group-buy venue. The platform consumer API now exposes tenant-input-free single-product detail and current VERIFIED, unexpired trace reports across linked merchants. Missing reports stay a truthful 404/empty presentation. H5 browser checks at 1024×768 and 390×844 found no horizontal overflow or console warnings/errors across the tested entry, navigation and fail-closed states. Because no local PostgreSQL/API consumer session or WeChat device was available, the normal data state and mini-program device acceptance remain unclaimed and all ten records keep `accepted: false`.

The next fourteen dedicated Life leaves are now implemented through one shared, page-ID-configured transaction journey component: PAGE-216/218/221 cover nearby stores, store detail and group-buy detail; PAGE-224/227/228/229 cover cart, fulfilment, reward disclosure, quote and idempotent order creation; PAGE-231/232 cover WeChat payment initiation and server-truth refresh; PAGE-235/237/238 cover membership, order list and order detail; PAGE-239/240 cover aftercare and refund detail. The component preserves platform-consumer isolation, never treats client payment completion as order truth and does not synthesize missing refund/order data. Representative H5 checks at 1024×768 and 390×844 passed exact frozen routing, no horizontal overflow, no console warnings/errors and the unauthenticated recovery path. All 14 remain `accepted: false` until authenticated and WeChat/device/provider evidence exists.

The final fourteen Life leaves are now independent routes: PAGE-219 maps only verified store coordinates; PAGE-242/243 expose server-signed remaining-use credentials; PAGE-245/246 submit and track aftercare; PAGE-248/250 validate encrypted address and invoice-profile writes with confirmed archival; PAGE-252 separates reward amounts; PAGE-259 links real orders to aftercare. PAGE-254/255/258/262/264 document and enforce the merchant-consumer session boundary instead of sending the platform token to merchant APIs. The matrix now records 43 designed/interactive cross-product leaves and no new accepted claims.

The GitHub code-quality job for candidate `46e39f0` exposed seven high-severity advisories in the frozen UniApp compiler's transitive graph while PostgreSQL, development-stack, Bao preview and all three container builds passed. Exact workspace overrides now pin patched compatible `@intlify`, `adm-zip`, `jpeg-js`, `postcss` and `ws` versions. The live production audit is back below the high threshold (one low and three moderate findings remain visible), and both Life H5/WeChat builds pass with the reviewed graph. These overrides are temporary until the frozen compiler train raises its own minimums.

## Completed in this stage

- Migration `0023` adds the tenant/store/user-bound employee Agent runtime: conversations, tasks, versioned steps, immutable artifacts/evidence and expiring approvals. Database constraints enforce 8-step conversational and 12-step background limits, at most two automatic retries, bounded tools/cost/deadlines, unknown-result fail-closed behavior and different-person dual confirmation. The audited target is now 153 tables; clean and incremental PostgreSQL CI include the new migration and fixture. Service, Worker and page integration remain the active next slice.
- Migration `0024` raises the audited target to 154 tables and adds immutable, RLS-protected group-buy publication receipts. The publish command serializes each tenant/idempotency key, enforces expected product version, assigned-store scope, active store, positive price, active variant and available stock, then records the real actor in audit. PAGE-088 now uses this MFA-protected boundary. Migration `0025` raises the target to 157 tables and adds RLS-protected customer-service shifts, resource-bound customer tasks and quality reviews. PAGE-103, PAGE-107 and PAGE-110 now use scoped duty, task completion and MFA quality-remediation commands with idempotency, version checks and audit. Migration `0026` raises the target to 164 tables and adds connector health, versioned reward rules, the ten official V1 skills, partner regions, model budgets and exact platform-control receipts. The final ten workbench pages now use authoritative, versioned and audited control APIs.
- Located and checksum-verified the V6.1 formal package.
- Read the external unique baseline and current `00`–`07` package entry points.
- Inspected the product boundaries, commercial rules, page tree, state machines, UI overview, PostgreSQL schema, OpenAPI, events, RBAC, test gates, repository strategy, and development instructions.
- Created the V6.1 rebuild branch.
- Recorded frozen development rules and known package conflicts.
- Vendored the verified `00`–`07` source package and repaired the generated migration's literal diff markers.
- Completed the V5→V6 keep/rewrite/migrate matrix; V5 remains recoverable from `main` and Git history.
- Replaced the V5 workspace with a pnpm modular-monolith foundation: shared contracts, Fastify API and RLS-safe Outbox Worker.
- Imported the 73-table PostgreSQL 15+ schema and added a real-database CI gate that attempts cross-tenant reads and writes.
- Added immutable audit/ledger checks, strict money/event contracts, deterministic builds, exact dependency locking, lint, type, unit-test and build gates.
- Implemented the first revenue vertical slice: unique merchant permanent-right activation, multi-holder 70% validation, transactional idempotency, audit and `revenue_right.activated.v1` Outbox publication.
- Implemented frozen subscription policy V1 (`70/10/20`) with deterministic minor-unit allocation that never loses a cent.
- Implemented statement math for receipts, refunds, actual/provisional/reversed direct costs, zero-floor loss handling and exact multi-party allocation.
- Added migration `0003` with deferred PostgreSQL constraints that reject locked statements unless shares total 100% and allocations reconcile to the cent; distribution entries remain append-only.
- Audited the package's distribution inputs and added migration `0004`: an immutable, provider-event-idempotent subscription receipt/refund ledger. The target now has 74 tables while the vendored source remains checksum-accounted at 73.
- Implemented transactional subscription statement locking. It derives amounts from the cash ledger and cost entries, resolves the active policy/right holders, allocates exactly, writes accruals/audit/Outbox/idempotency atomically, and never accepts client-supplied money.
- Published a deliberately implementation-bounded OpenAPI document and added syntax/reference drift checks.
- Corrected minor-unit residual handling so subscription rounding tails always enter the `LEQU_LIFE` allocation rather than a generic largest-remainder recipient.
- Added migration `0005` with dual-control distribution approvals, immutable provider payout evidence, exact original-entry linkage and append-only payout/reversal ledger constraints. The audited target now has 76 tables.
- Implemented separate request, different-person approval, payout and reversal commands. Every command has its own idempotency scope; finance membership is revalidated at approval and execution; payment requires one unique provider-reference hash per positive allocation.
- Implemented `ACCRUAL -> PAYMENT -> REVERSAL` linkage for paid statements and `ACCRUAL -> REVERSAL` for unpaid statements, with audit and financial Outbox events committed in the same transaction.
- Expanded the implementation-bounded OpenAPI from five to nine paths.
- Added a signed session identity boundary for the new merchant-intake APIs. Tenant, actor and roles come from an HS256 Bearer token and active database membership is revalidated on every command.
- Added migration `0006` and the immutable `merchant_intake_commits` snapshot. Candidate inserts now require a same-session asset whose security and processing states are `SAFE/SUCCEEDED`; confirmations and commits are append-only. The audited target now has 77 tables.
- Implemented the trusted AI merchant-intake domain slice: idempotent session creation, immutable asset metadata, security rejection, safe extraction results, multi-source conflict retention, typed human confirmation and atomic formal merchant-profile commit with audit and Outbox.
- Expanded the implementation-bounded OpenAPI from nine to fourteen paths.
- Added migration `0007` and tenant-scoped upload tickets. Object keys, SHA-256, MIME type, byte ceiling and expiry are signed; completion trusts only storage-side HEAD evidence and consumes a ticket exactly once. That stage raised the audited target to 78 tables; the current post-`0026` target is 164 tables.
- Added the ordered intake processing boundary: malware verdict precedes OCR/document/ASR extraction, unsafe material never reaches a model, and structured candidates can only be persisted through the trusted application service. HTTP adapters validate upstream responses and turn extraction outages into retryable failed assets without deleting raw material.
- Added text-message ingestion through deterministic tenant object keys and the same immutable asset queue.
- Added verified enterprise WeCom internal-app callbacks: callback freshness, SHA-1 signature, AES-CBC/PKCS#7 decryption, corp identity, member binding, message/payload idempotency and expired-media retry evidence. It does not read or answer employee personal WeChat contacts.
- Replaced the old client-supplied object-key endpoint with upload authorization and completion endpoints. The implementation-bounded OpenAPI now has seventeen paths.
- Accepted ADR-0004 and added the only active 乐趣宝 Web runtime for PAGE-014 and PAGE-175–178. PC and mobile H5 share state and confirmation rules, with explicit loading, empty, recoverable error, denied, stopped and success presentations.
- Connected the Web runtime to the implementation-bounded APIs in non-demo mode: signed employee session bootstrap, existing/new intake loading, text ingestion, SHA-256 upload-ticket flow, storage PUT completion, candidate rendering with HTML escaping, legal confirmation and guarded commit. Demo mode remains explicit and cannot be mistaken for a live API session.
- Completed browser acceptance for PAGE-014 and PAGE-175–178 across 1440×900, 1024×768 and 390×844, including the six state presentations and the send, field-review, voice-toggle, retry and legally gated commit interactions. Fixed the 1024×768 composer visibility defect and recorded the evidence in `docs/acceptance/2026-08-18-merchant-intake-browser.md`.
- Audited the complete V6.1 launch inventory and mapped all 143 acceptance cases and all 307 page nodes/197 executable leaves into a ten-stage release plan guarded against drift.
- Added migration `0008`: revocable device-bound sessions, immutable entitlement snapshots, immutable AI usage facts and a deterministic seed generated from the frozen 15-role/51-permission/213-scoped-grant matrix.
- Replaced token-only trust with live database authorization on every implemented protected API. Tenant, user, current roles, store scope, membership, account/risk status, MFA and tenant write state are revalidated on each request.
- Added rotating refresh credentials stored only as SHA-256 digests, explicit revocation and organization switching derived from current server-side membership. Initial sign-in now consumes a one-time enterprise-WeCom or phone-OTP assertion through a production-required identity gateway; tenant, user and MFA level are gateway-owned and device/freshness bound, with no local password substitute. The gateway contract now requires an allow decision and rate-limit policy evidence, receives only server-derived IP/user-agent risk context through an exact trusted-proxy allowlist, and maps throttling separately without account enumeration. Successful initial sessions and tenant switches write same-transaction audits containing keyed hashes rather than raw network, user-agent or assertion identifiers.
- Added immutable entitlement snapshot and idempotent AI usage-meter services, including hard-limit enforcement and conflict-safe replay.
- Added frozen compute-package and voucher commission/refund policies, including exact minor-unit residual handling and append-only negative reversals.
- Added migration `0009` and application services for beneficiary-confirmed, different-person-approved revenue-right transfer and multi-party dispute freeze. PostgreSQL trigger fixtures are wired into CI.
- Expanded the implementation-bounded OpenAPI to 24 paths. The full local repository gate passes formatting, lint, 83-table contracts, RBAC generation, release-plan mapping, OpenAPI, all TypeScript checks, 92 unit tests and production builds.
- Added migration `0010` and five tenant-scoped delivery evidence tables. Delivery attempts become immutable after one terminal transition; acceptance receipts and status history are append-only; partner project grants expire within 30 days and never include customer or finance scopes.
- Implemented the frozen 38-step `merchant_delivery_standard` workflow with version snapshots, exact prerequisite checks, resumable pause/start, targeted retry, partial-success preservation, provider-unknown blocking, sanitized exceptions, separate platform/merchant/external timers and an exception center.
- Enforced merchant confirmations at both service and PostgreSQL boundaries. Payment binding, first-offer price/refund rules, mini-program name, WeChat-review disclosure, review submission and release cannot be bypassed by direct API calls.
- Added immutable delivery acceptance: every required step must be `SUCCEEDED`, checklist/step snapshots and acceptor IDs are hashed and stored, and the database rejects premature `DELIVERED` transitions.
- Restricted regional-provider intake, delivery detail, exception and execution access to live project assignments with the required material/step scope. Expired or revoked assignments behave as not found.
- Expanded the implementation-bounded OpenAPI to 34 paths and wired the delivery workflow PostgreSQL fixture into clean-schema CI.
- Added migration `0011` and five tenant-scoped mini-program lifecycle tables. AppID hashes are globally unique, provider secrets must be secret-manager references, exact builds and merchant confirmations are immutable, release transitions are database guarded, external attempts terminate once and authorization loss stops further operations.
- Implemented the merchant-owned shared-template lifecycle: one-time authorization exchange, canonical configuration digest, deterministic experience build, assigned-publisher eleven-item confirmation, review submission and exact callback replay, rejection continuation, approved-only release, timeout-to-online-query recovery, staged rollout health and history-preserving rollback.
- Added a production-shaped HTTP boundary for the component-platform gateway and builder. Safe-mode callbacks validate SHA-1 and freshness before storing the encrypted original, then delegate decoding to the trusted gateway; secrets and decrypted payloads are not written to logs or durable application rows.
- Connected nine mini-program paths to live session/MFA/RBAC enforcement and the stage-3 delivery workflow. The implementation-bounded OpenAPI now has 43 paths and the PostgreSQL lifecycle fixture is wired into clean-schema CI.
- Added migration `0012` and eight tenant-scoped customer-service/privacy tables. Consumer sessions, knowledge publications, AI jobs, tool calls, answer evidence, profile facts, privacy requests and notification attempts are RLS protected; published knowledge, messages, citations, tool evidence and consent history are immutable or one-way terminal.
- Implemented the self-hosted customer-service path for merchant mini-programs and 乐趣生活: independently signed consumer identity, durable object-first messages, source-grounded AI answers, read-only live business tools, prompt-injection scope checks, confidence/no-source handoff and late-answer cancellation after human takeover.
- Implemented deterministic high-risk routing, one active handoff per conversation, atomic employee claim, true employee sender identity, explicit return-to-AI, in-app notification durability and optional internal WeCom notification without personal-WeChat access. Full-message reads revalidate consumer/store scope and every employee正文 read is audited.
- Implemented merchant knowledge governance for READY, non-personal documents: store-scoped immutable publication versions, trust levels, activation/expiry and audited revocation. Cross-tenant or expired retrieval cannot be used by the model.
- Implemented continuous customer-profile control: purpose-specific append-only consent, verified fact evidence, source/confidence/expiry metadata, consumer-visible values, correction/delete/restrict queues and immediate blocking of new or existing personalization after withdrawal/restriction.
- Connected the tenant-partitioned AI Worker and production-shaped knowledge, model, business-tool, object-store and internal-WeCom adapters. The implementation-bounded OpenAPI now has 57 paths and the PostgreSQL customer-service fixture is wired into clean-schema CI.
- Added migration `0013` and ten commerce evidence tables, bringing the audited target to 111 tables. Database-final row locks and append-only guards enforce inventory reservation/release/consume, order and payment transitions, refund item/header scope, verification token/store/time/quantity checks, balanced reward entries and immutable reconciliation evidence.
- Implemented server-priced consumer orders, confirmed merchant-direct payment intents, exact raw-body signed callback convergence, partial/full aftercare refunds with snapshot approval policy, opaque verification tokens, linked reward grants/reversals and explainable daily reconciliation that stops on one-cent or missing-record differences.
- Added production-shaped payment/refund/reconciliation adapters, exact JSON callback body capture, 11 authenticated/public/internal HTTP paths and tenant-scoped worker jobs for order expiry and refund submission. Unknown payment-create results recover only with the same intent and provider idempotency key.
- Covered ORD-001–003, PAY-001–005, REF-001–004, VER-001–003, REW-001–004 and REC-001–002 across service tests and the PostgreSQL fixture. The implementation-bounded OpenAPI now has 68 paths; the full local gate passes with 141 API tests, 168 repository tests and all production builds.
- Added buildable `consumer-miniapp` and `merchant-miniapp` packages. 乐趣生活 fixes the five frozen consumer tabs, while both packages map all 38+24 executable mini-program leaves into route contracts with all eight required states. Launch audit found that the initial surfaces were prototypes; follow-up remediation removed static commerce results, connected seventeen store/catalog/cart/checkout/order/payment/address leaves to authenticated APIs and made the other 45 routes fail closed outside explicitly labelled demo mode.
- Reused and mobile-optimized the formal life banner, product, local-dining and 16-category assets into a 180,830-byte repository-owned set. A visual gate checks the semantic sprite, all 14 key renders, exact leaf counts and sub-megabyte asset budget; generated mini-program packages are approximately 235 KB and 219 KB.
- Implemented consumer/merchant order-payment-refund client boundaries that never trust provider-side payment success, explain separated fulfilment groups and prevent duplicate pending aftercare. The merchant AI clerk explicitly identifies AI, requires identity binding before order lookup and exposes human handoff.
- Completed the frozen Harness task/result/source right rail in 乐趣宝 with interactive three-tab switching, close and reopen behavior. UI-001–UI-005 now have local automated or semi-automated evidence. The full local gate passes 175 tests and all six buildable workspace outputs.

## Verified package facts

- 583 checksummed files, zero checksum mismatches.
- 307 page nodes and 197 leaf pages.
- 73 PostgreSQL tables, 37 target OpenAPI paths, 46 domain events.
- 15 roles, 51 permission controls, and 143 acceptance tests.
- The existing V5 repository has 134 OpenAPI paths and approximately 136 SQLite tables; replacement requires an explicit compatibility and migration matrix.

## Current blockers and risks

- The delivered migration contained literal `+` patch markers. The repository copy and generator are repaired and guarded against recurrence.
- The frozen `70/10/20` distributable-income policy conflicts with a separate `25%` channel-distribution statement. V6.1 development uses the frozen versioned policy; production payout needs written business and finance resolution.
- Current material mixes `商务人员` and `商务推广人员`, and mixes `生活消费` and `生活`. The external unique baseline wins pending formal amendment.
- The target stack recommendation differs from the V5 implementation. The rebuild must use architecture decision records and must not mix package managers or duplicate long-lived app implementations.
- The enterprise/phone identity-provider assertion adapter and initial-session endpoint are implemented and production-required. Real gateway credentials, one-time assertion consumption and controlled device/MFA evidence remain launch gates; production has no local identity bypass.
- Failed provider attempts are evidence-bound and blind external retries are blocked. Payment-create timeout recovery uses the same provider idempotency key; unknown refund-result query orchestration and real payment connector sandboxes remain required before launch.
- Object storage, malware, OCR, speech and structured-extraction adapter contracts are implemented and fault-tested, but no production provider credentials were supplied; real provider calls remain an integration gate.
- Enterprise WeCom cryptography, tenant/member binding and receipt idempotency are implemented and simulated with official message framing. A real enterprise application callback and expired-media download still require controlled credentials.
- PAGE-014 and PAGE-175–178 passed evidence-backed browser acceptance on PC and mobile viewports, including loading, empty, recoverable error, denied, stopped and success states. No browser console warning/error or horizontal overflow was observed.
- Stage 8 adds migration `0014`. GEO submission and weekly monitoring retain per-target evidence, accessibility state, provider retry timing and canonical-field differences while prohibiting inclusion/ranking/traffic promises.
- Monthly value reports are immutable snapshots whose metrics include versioned definitions, generation cutoffs and exact source event IDs. Official plugins are first-party/published only and enforce exact owner-approved grants, declared HTTPS domains, denial audit, independent circuits, reauthorization on scope expansion and uninstall deletion receipts.
- Three pilot templates cover dining, local retail and leisure services; local PostgreSQL execution is complete, while external GEO/plugin gateway drills remain Stage-10 controlled gates rather than simulated evidence.
- Stage 9 adds migration `0015`, resumable/hash-bound legacy migration, three-level zero-difference switching, safe consent/identity review, multi-target privacy deletion and restoration replay. Security headers, trace IDs, redaction, authenticated metrics, 14 mandatory alert rules, selective freezes, deterministic CycloneDX SBOM, secret scanning and dependency audit are launch gates.
- Encrypted fresh-database restore, expand-only rollback and performance harnesses are checked in. A real local encrypted logical restore now meets RPO/RTO, financial-digest, privacy-replay and 22-fixture checks; physical/WAL cross-fault-domain recovery, the independently reviewed V5 zero-data inventory/greenfield waiver, pre-production performance and on-call delivery evidence remain Stage-10 controlled-environment gates. Any discovered V5 production record invalidates the waiver and restores the full production-size migration requirement.
- Stage 10 expands 乐趣宝 contract coverage from five intake pages to all 135 frozen leaf routes and all eight required states. PAGE-014 and PAGE-175–178 remain the domain-integrated intake workflow. Another 130 launch-scope pages now read authoritative APIs for identity, sales/subscriptions, bounded Agent tasks/approvals, operations, GEO publication/differences, intake/data confirmation, customer-service duty/tasks/quality, delivery, mini-program release, rewards, plugins/skills, merchant/plan/partner/model administration, organization/audit/privacy, revenue/cost/appeals, knowledge/reports and merchant commerce. Production still fails closed without a token, resource ID or implemented API.
- A full original-design launch audit now tracks all three products: all 197 P0 leaves are authoritative and none remains prototype-only or contract-only. All 62 mini-program leaves are authoritative; `docs/release/LAUNCH_READINESS_AUDIT.md` is the current release-scope truth source.
- The post-`0026` full local gate is green: formatting, lint, contract/RBAC/release/acceptance/visual/workbench/OpenAPI/security/operations/deployment checks; all six workspace type checks; 79 test files and 401 tests, including complete evidence-manifest, exact Workbench leaf coverage, Worker publisher, production UI policy, runtime configuration, multi-tenant WeCom resolver and production Web-server tests; and all six production builds. Reward rules are frozen into each order item at order creation, so payment callbacks never read mutable product policy. Cross-merchant platform search executes only through a security-definer database boundary that independently validates active platform membership and role. The controlled performance gate now refuses production-shaped targets, requires all three frozen scenarios, proves acknowledged customer messages in PostgreSQL and writes a non-overwriting report with latency, database and Outbox evidence. Backup/restore drills now bind the encrypted artifact to a per-tenant financial digest, verify privacy replay and every database fixture, enforce the five-minute RPO/sixty-minute RTO and emit a durable report. Browser acceptance at desktop and 390×844 mobile widths found no horizontal overflow or console warnings/errors and kept missing data/resources truthful.
- A real local API/PostgreSQL performance rehearsal repaired the last known harness-start blockers: scenario-specific employee/consumer/write identities replace one impossible shared token, the Outbox age query uses valid PostgreSQL aggregate filtering, and runtime failures emit a redacted durable FAIL report. The 5-concurrency/20-request-per-scenario success run had zero errors, met all three P95 thresholds and found 20/20 acknowledged messages in PostgreSQL. Production-shaped controlled performance is still required.
- PAGE-012 now has dedicated PC and 390px mobile identity/workspace surfaces backed by the signed `/api/v1/context` response. Switching uses the existing server-issued tenant session boundary, revalidates live membership/roles/store scope, replaces the client session only after a complete response and keeps the old session on failure. The page does not enumerate cross-tenant memberships, expose credentials or enable switching in demo mode; controlled PostgreSQL and real-identity acceptance remain unverified.
- PAGE-015 now has a dedicated responsive material-and-voice capture surface over the existing tenant-scoped intake session. It shows accepted file/audio types, malware and processing states, SHA-256 evidence and field references; production uploads still use the digest-bound upload-ticket flow, while demo mode cannot upload or write text. PC and 390px browser layouts passed local inspection; real object-storage, malware and speech providers remain controlled integration gates.
- Production-entrypoint review found that Worker `main.ts` claimed Outbox rows without ever publishing or settling them. The entrypoint now requires a safe event gateway before claiming, publishes the full tenant/event envelope with event-ID idempotency, classifies responses without reading provider content, settles every outcome and reclaims crash-stale `PROCESSING` locks after five minutes. The Worker unit suite and real PostgreSQL runtime publish/stale-recovery integration pass.
- Portable production-only API and Worker deploy outputs passed equivalent runtime smoke: API `/health` and `/ready` were green, and Worker published/settled 20 events with no `PROCESSING` residue. `deploy/Dockerfile` provides non-root API/Worker/Web targets, the Web production server passed SPA/health/security-header tests, and CI now builds all three images plus a Web container smoke. Local Docker execution remains unavailable on this workstation and must be proven by the Linux CI job before freeze.
- A final production-path audit removed sample employee, merchant, task and progress data from non-demo Workbench rendering, made shell/mobile navigation route to authoritative pages and blocked browser-only intake success when no live service/session exists. Browser re-acceptance found no leaked demo names, fixed counters, horizontal overflow or console warnings/errors. API and Worker now reject partial provider groups in every environment and refuse production startup unless every launch-flow identity, payment, AI, GEO, mini-program, WeCom, internal-job and privacy gateway is configured; the container CI verifies both fail-closed startup boundaries.
- Enterprise-WeCom intake no longer binds one CorpID/member/tenant through process environment. A hardened HTTPS adapter resolves CorpID to secret-manager-backed callback configuration and then resolves each member to the current tenant/user/store/session identity; 404 remains unauthenticated, cross-Corp responses fail closed and provider bodies are not copied into errors.
- Real local PostgreSQL 15.19 execution now passes clean schema creation (164 tables/26 migrations), all 22 database fixtures, the formal 73-table baseline upgrade through `0026`, distribution locking, repeat-safe merchant intake, a 10-contender/3-stock commerce concurrency run and Worker tenant/retry/Inbox/gap/dead-letter/replay fault injection. A real `age`-encrypted logical backup also restored to a fresh database with matching ciphertext and financial digests, one privacy replay, RPO 97.01 seconds, RTO 15.04 seconds and isolated 22/22 fixture verification. The run repaired migration/schema defects, fixture/integration drift, UUID/text Outbox parameter typing and restore-script isolation/time parsing; CI now includes the commerce and Worker integrations. See `docs/release/stage-10-postgresql-local-evidence.md`. This is strong local engineering evidence, not the independently reviewed controlled result manifest.
- Consumer catalog reads validate the live consumer session in PostgreSQL, set tenant scope per transaction and expose only active-store, on-sale product data. Store contact/address ciphertext and object references never enter the response. Both consumer and employee JWT verifiers now reject non-canonical Base64URL encodings even when they decode to the same signature bytes.
- Migration `0016` raises the audited target to 127 tables and adds ordered consumer offsets plus durable dead-letter evidence. Worker delivery now supports exact retry policy, Inbox deduplication, aggregate-version gap detection, twelve-attempt dead-lettering and explicit replay of the original event ID.
- Migration `0017` raises the audited target to 132 tables and separates platform consumer identity from merchant-bound customer sessions. Account-scoped forced RLS protects revocable life sessions, active account-to-merchant links and one cross-merchant cart; authenticated cart reads reprice each item inside its owning tenant. Platform order list/detail queries traverse only active customer bindings and never accept a client-selected tenant.
- Platform aftercare now resolves the owning merchant/customer/store exclusively from the authenticated platform account and order, then independently revalidates that link inside refund and verification services. 乐趣生活 exposes persisted refund progress, safe refund application, available vouchers and reward grant/reversal projections. The merchant template uses its tenant/customer/store-bound consumer session for order list/detail, merchant-direct payment, refund history and voucher credentials.
- Existing workbench delivery, mini-program and customer-service detail surfaces now expose fixed, page-specific write commands with fresh idempotency keys and explicit confirmation. Unsupported generic buttons are disabled rather than simulating success. Workbench tests are 29/29; local browser acceptance confirms unauthenticated fail-closed behavior, explicit demo labeling and no desktop/mobile horizontal overflow or console errors.
- Migration `0019` adds immutable checkout source classification for the independent merchant mini-program path. Merchant PAGE-280/282 use an exact dual-session customer mapping and server-derived tenant/store scope from persistent cart through order creation. PAGE-269, PAGE-297–299, PAGE-304–305 and PAGE-307 use authoritative store, AI/human customer-service, refund and privacy APIs. PAGE-270 atomically rotates a same-expiry store-bound consumer session and PAGE-302 reads merchant membership plus the isolated reward ledger projection. All 24 merchant-template leaves are now API-connected.
- Migration `0020` raises the target to 139 tables with versioned verified product-trace reports and account-isolated encrypted invoice profiles. 乐趣生活 now connects category/search, merchant discovery, membership, customer-service/handoff, privacy/consent, subscription messages, tickets, product provenance and invoice profiles. Optional nearby location is response-only and discovery is bounded to active merchant links. All 38 乐趣生活 leaves are now API-connected.
- Migration `0018` raises the audited target to 137 tables and adds encrypted platform addresses, versioned store checkout policies, active automatic discount rules and account-isolated checkout/group evidence. Server quotes reject stale carts and changed inventory/price, calculate merchant-specific shipping and best eligible discount, and idempotently create one order per merchant/fulfilment group. Partial failure keeps successful groups and retry keys; the cart closes only after every group order exists. Reward redemption stays explicitly unavailable until the frozen financial policy is signed.
- 乐趣生活 PAGE-231/232 now resolve an order only through active platform customer bindings and create payment credentials through the owning merchant's confirmed account. The commerce payment service independently revalidates the platform session/link, preserves provider idempotency recovery and updates success only from a verified callback. The mini-program treats provider acceptance as provisional and queries the server order state before showing paid.
- PAGE-248 now owns encrypted platform delivery addresses. Recipient, mobile and street values use AES-256-GCM with a random nonce per write, remain account-scoped under forced RLS and are unavailable when the secret-manager key is absent. Physical checkout uses an authenticated default address; other fulfilment types do not invent one.
- Migration `0021` raises the audited target to 146 tables and adds owner-scoped sales opportunities, immutable duplicate checks, versioned quotes, signed contracts, immutable collection receipts, dual-controlled subscription changes and report-backed renewal previews. Stable merchant identifiers, signer references and provider references are HMAC-only. Activation and renewal require signed contracts whose net confirmed collection covers the contract; the requester cannot approve, and only the internal worker applies an approved due change. PAGE-026–028, PAGE-030–031, PAGE-045–046, PAGE-049–050 and PAGE-145 now use these APIs through page-owned forms that keep sensitive values out of URL history. Targeted sales/subscription service and HTTP tests pass 24/24; the API suite passes 271 tests, API type checks/build pass, Workbench tests pass 15/15, OpenAPI contains 155 paths and the 146-table static contract gate passes. The migration and its fixture are now included in the successful PostgreSQL 15.19 clean and incremental runs.
- PRI-001 has a fail-closed privacy export Worker contract requiring KMS envelope encryption, at most fifteen minutes, verified-session delivery and audit evidence.
- All 143 acceptance IDs are evidence-mapped: 111 local automated, 3 locally reviewed and 29 controlled-environment pending. `pnpm launch:gate` fails without a controlled result manifest and then verifies its exact candidate commit, current plan hash, all 11 suite results, independent reviewer roles, declared artifact set and each artifact SHA-256 before it can turn green.
- The 29 controlled-environment cases are now mapped exactly once across nine executable suites, with two additional external-only suites for official WeChat release/rollback and identity/secrets/privacy/on-call infrastructure. The acceptance verifier checks suite uniqueness, all seven external gates, executor roles, runbooks, safe evidence directories, required artifacts and pass criteria; this is an execution plan, not substituted PASS evidence.
- The last full local gate before migration `0021` was green. The current incremental evidence is green for 146-table contracts, API/Workbench type checks and builds, 271 API tests, 15 Workbench tests, 155 OpenAPI paths and the sales/subscription targeted suites. A new full-repository gate and browser acceptance remain required before this checkpoint can be called locally green.
- The 2026-08-20 post-hardening full local gate is green: 103 test files/528 tests, eight type/contract checks, six production builds plus the post-build artifact inventory, 193 OpenAPI paths, 525 scanned text files and all static security/operations/deployment checks. The launch gate remains correctly closed because no controlled result manifest or real Stage 47–50 infrastructure exists.
- The first controlled greenfield-discovery action found one ignored V5 default SQLite file in the local project and no committed V5 database, repository Deployment or repository Release record. The local file is non-empty (136 application tables, 123 non-empty tables and 1,520 rows) and has strong development-demo provenance, but that provenance is not an independently reviewed deployment-ownership decision. A new read-only inventory tool hashes file/location evidence, emits table-level counts only, refuses overwrite, stops on non-empty production/unknown sources and never grants a greenfield PASS. The local result remains `DATA_PRESENT_REVIEW_REQUIRED`; external hosts, volumes, object stores, provider ledgers and container packages remain unverified.
- Completion audit identified two baseline launch gates that were described but not previously mandatory in the controlled manifest. The payment suite now requires a signed `financial-policy-approvals.json` covering payment responsibility and both frozen finance decisions. The identity/privacy/on-call suite now requires `legal-document-release.json` proving approved, live and version-bound user agreements, privacy policies and required compliance texts. The acceptance verifier prevents either requirement from being silently removed.
- Controlled result manifests now use schema version 3. Every suite requires different opaque accountable executor/reviewer subjects and an ordered review timestamp after execution. Every evidence record binds both the artifact hash and its candidate/plan/deployment/environment capture-receipt hash. The non-overwriting assembler consumes complete reviewed decisions and immediately runs the launch verifier; it never creates a PASS decision itself.
- Controlled evidence is now inspected before both assembly and final launch verification: files must be bounded, non-empty UTF-8 text; JSON must be non-empty and parseable; placeholder verdicts, binary content and common unredacted credential forms fail closed. SHA-256 still binds the accepted bytes, and independent suite-specific review remains mandatory.
- Controlled execution now starts with a non-overwriting workspace initializer. It requires the exact clean candidate checkout and a new absolute destination, binds the candidate/plan/deployment/environment, creates all 11 suite directories and a generated 47-artifact/runbook checklist, and emits only `PENDING` decision metadata—never evidence placeholders or PASS decisions.
- Each checklist slot now has an exact generic capture command. The capture boundary revalidates the clean candidate and plan hash, allows only declared suite/artifact pairs, scans the source for content/redaction failures, writes both artifact and bound receipt with exclusive-create semantics and rechecks destination bytes against the inspected hash. Assembly and final launch verification reject missing, changed or semantically forged receipts even when the receipt hash is updated; direct overwrite or capture bypass is prohibited.
- All 39 controlled JSON artifact names now have minimum domain-semantic contracts, and the acceptance gate proves contract coverage stays exactly aligned with the plan. Generated workspaces expose required dotted fields/types plus PASS, true and zero-unresolved invariants. Capture, assembly and launch verification share the validator, so unrelated but parseable JSON or empty proof collections cannot satisfy a slot. Capture receipts now also require and bind the actual byte size; the earlier undefined `size`/`sizeBytes` mismatch is covered by regression tests.
- Semantic contracts now bind every artifact-level `releaseCommit` and `deploymentId` to the prepared workspace and enforce operational ranges and formats, including HTTP success, positive server amounts, non-negative inventory, RPO ≤ 300 seconds, RTO ≤ 3,600 seconds, 64-character hashes, immutable GHCR digests and non-future ISO timestamps. A self-consistent hash package for a different candidate or an internally contradictory PASS report therefore fails before review.
- Seven suites now have cross-artifact reconciliation at assembly and final launch: commerce input/order/ledger quantities, payment request/callback/reconciliation/refund account identity and amounts, backup/restore filenames, performance report/topology/candidate image digests, official WeChat build/publish/rollback versions, delivered/on-call-acknowledged alert identifiers and intake upload/metadata object identity. The generated workspace lists these invariants, and a test proves that updating an artifact, its receipt and both result hashes still fails when the changed file contradicts the rest of its suite.
- Candidate deployment now has a locally verified protected image-publication workflow. From trusted `main` and only after `controlled-preproduction` review, it verifies the exact candidate's code-quality/PostgreSQL/container checks, publishes non-root API/Worker/Web images to GHCR with unique run tags, BuildKit provenance and SBOM attestations, and retains a three-digest deployment manifest. It is not yet on `main` and has not published images.
- Candidate checks are no longer trusted by display name alone. The publisher and final verifier execute a trusted provenance checker that requires an identical trusted/candidate CI workflow, GitHub Actions ownership, the exact candidate SHA, the repository's CI workflow path, an allowed push/PR event and same-repository workflow metadata. The checker also passes against the real `d98631e` PR run metadata and rejects same-name results from another app, commit, repository, workflow or event.
- Every external Action in all four workflows is pinned to a reviewed 40-character commit rather than a movable major-version tag. A central allowlist records the seven approved upstream revisions, and the deployment gate rejects unknown Actions, version tags and changed SHAs; version comments remain beside each pin for maintainable upgrades.
- The external Dockerfile frontend, production build/runtime Node images, CI PostgreSQL service and PostgreSQL-backed Dev Container are pinned as readable tags plus immutable Docker Hub multi-architecture manifest digests. A central five-image allowlist and deployment test reject movable syntax frontends, bare tags, unknown bases and changed SHA-256 values while continuing to permit named internal Docker stages.
- Final controlled approval now has a repository-owned protected workflow design. It accepts only a candidate-bound draft Release containing one canonical uploaded asset, locks the immutable Release/Asset IDs, downloads through the Asset ID, validates paths/types/sizes before extraction, runs verifier code from trusted `main` rather than candidate code, records archive/candidate/results/plan hashes and creates an OIDC-backed attestation under `controlled-preproduction` approval. This is locally tested configuration only until reviewed onto `main` and executed with real Stage 47–50 evidence.
- Controlled release packaging now has an exact-inventory boundary. A non-overwriting staging command revalidates the candidate/result bundle and copies only the manifest, execution context, 47 artifacts and 47 capture receipts; operator files and undeclared content are excluded. The protected verifier independently rejects missing or extra extracted files before launch verification, preventing an otherwise valid evidence bundle from uploading accidental attachments.
- The production-shaped performance gate now requires the protected candidate image manifest, a deployment-platform snapshot of the running API/Worker/Web digests and the exact release SHA. All three deployed digests must match before load is sent, and the binding is written into the non-overwriting report. A read-only 2026-08-20 environment-name audit confirms Stage 49 coverage at 7/32; the Worker tenant UUID and both runtime image snapshots remain unavailable until controlled provisioning/publication/deployment.
- Controlled preflight now covers every API and Worker production-required setting, including `WORKER_TENANT_ID`, and shares their fail-closed constraints for PostgreSQL TLS, 32-byte JWT/signing secrets and the address-encryption key. The protected workflow maps every Stage 47–49 name and rejects a configured release SHA that differs from the immutable requested candidate, preventing a green preflight followed by deterministic production startup failure.
- Stage 49 preflight now reuses the actual performance configuration validator when its performance subset is complete. Candidate manifest fields/SHA, all three immutable GHCR digests, deployed-image equality, non-production HTTPS target, same-origin API paths, secret-free JSON bodies and the report path are rejected before any load can be sent; the performance gate remains the authority for executing and measuring the run.
- Stage 49 image provenance is no longer a self-reported JSON field. The protected preflight accepts a publisher run ID, binds it to the current trusted-policy SHA and same-repository `workflow_dispatch`, requires exactly one unexpired candidate/run-named Artifact, locks its immutable Artifact ID, verifies GitHub's SHA-256 over the downloaded ZIP, safely extracts its sole root manifest and requires normalized equality with the configured performance manifest before load approval.
- A single development-only mock gateway now covers identity, object storage, commerce, customer-service, WeCom, GEO/plugins, mini-program, Outbox and privacy provider contracts with explicit `development-mock` provenance. It requires opt-in, and the gateway, API, Worker and controlled preflight all reject mock mode in production/controlled execution. This unblocks application development without converting mock behavior into Stage 47–50 evidence; PostgreSQL remains a real local dependency.
- The development Mock profile is now mechanically checked rather than trusted as a copied example: all 45 production runtime keys plus four explicit Mock controls must be present without undeclared drift, every provider URL/token must resolve to the same loopback-only gateway, the database must remain PostgreSQL and the address key/Worker tenant must retain their runtime shapes. The checked-in example and ignored local profile both pass; a live listener passed health, provenance, unauthorized rejection and MFA identity exchange, while the complete 24-route provider matrix is covered by automated tests. None of this evidence is accepted by the controlled launch verifier.
- A GitHub Codespaces/Dev Container workspace now supplies PostgreSQL 15, fail-closed clean-schema initialization, an opt-in/idempotent non-financial development tenant seed, generated ignored mock configuration and managed Mock/API/Workbench processes. The development Web server proxies API requests same-origin and exposes an opt-in local login that still traverses the identity gateway and database membership/role checks; the production server has no such route. PostgreSQL CI applies the seed twice in an isolated database and proves both idempotency and zero financial facts. The `development-stack` job builds the actual workspace, boots all three services, creates a PostgreSQL-backed session, verifies 12 roles and store scope through the Workbench proxy, then stops processes and deletes its isolated volume. Both push and PR executions for commit `37ab108` passed this real PostgreSQL-backed cloud-development path.
- The V6.1 candidate is merged to `main`; the merge CI passed code-quality, PostgreSQL, development-stack and all three container jobs. Reviewed PR #2 previously placed the standalone preflight on `main`. The protected `controlled-preproduction` environment still prevents self-review and accepts only the trusted `main` workflow ref; because this is now an explicitly solo-maintained repository, its GitHub approval policy must be revised separately before protected publication instead of claiming that a second GitHub reviewer exists.
- Commit `4cdc30e` is published on `upgrade/v6.1-rebuild`; both push and PR runs pass code-quality, PostgreSQL contracts, the real PostgreSQL-backed development stack and all three non-root container builds. Stage 47–50 provisioning now also has a canonical names-only inventory derived directly from the protected workflow. It classifies each required secret, variable and external evidence file, audits per-stage coverage without reading values, and fails until every required name exists.
- Dependency installation is now fail-closed and repository-governed: pnpm/Node are pinned, every CI/container/cloud install is frozen, direct dependency versions and lockfile SHA-512 integrities are checked, exotic transitive sources and lockfile trust bypass are rejected, and only the reviewed `esbuild` lifecycle script may execute. Frozen offline installation reports no ignored builds; the dedicated four-test policy suite and the full 103-file/528-test gate pass.
- Production dependency review now covers both known vulnerabilities and licensing rather than treating the SBOM as sufficient evidence. CI's exact-lockfile `pnpm audit --prod --audit-level high` currently reports no known vulnerability. The local and CI security gate independently inventories 65 production package versions, accepts only the reviewed permissive SPDX allowlist and rejects unknown, unlicensed, source-available, copyleft, empty, malformed or contradictory reports; the current graph contains only BSD-3-Clause, ISC and MIT.
- Production packaging is now runtime-only. API and Worker builds emit JavaScript without source/declaration maps, both package manifests expose only `dist`, and dry-run package inventories prove 65 API JavaScript files plus `package.json` and 12 Worker JavaScript files plus `package.json`—no source, tests, TypeScript configuration or map files. The full gate now runs a post-build artifact policy that checks regular-file type, entrypoints, extensions, test/hidden names, source-map references, secret-shaped values and size/file ceilings across API, Worker and Web; the current aggregate is 85 files and 1,353,823 bytes.
- Production Workbench API traffic is now mechanically same-origin: hostile `apiBase` query values are ignored and the API client rejects cross-origin destinations before attaching an employee Bearer session. The production server additionally canonicalizes static paths to stop symlink/junction escapes, makes health depend on the real immutable index, rejects unsupported health methods and emits stricter cross-origin resource headers. Local browser acceptance rendered the truthful denied state with no console errors or horizontal overflow.
- Stage-50 governance evidence no longer accepts hollow arrays or arbitrary verdict strings. Financial policy requires separately accountable business/finance approvals, every frozen decision resolved and a later different reviewer; V5 inventory rejects unknown/stop-release sources; the greenfield waiver requires accountable zero-record coverage across five infrastructure categories and ten business domains plus four distinct owner roles; legal release requires immutable HTTPS documents bound to all three product surfaces and different product/legal approvers. Capture, assembly and final verification share these deeper rules, and generated execution workspaces print them for operators.
- Disaster-recovery and performance PASS labels are now recomputed from domain facts. Restore evidence repeats and cross-reconciles the encrypted backup and financial snapshot hashes, carries the exact current fixture set and privacy replay, and is paired with cross-fault-domain WAL and four-target deletion proof. Performance evidence must contain the exact three frozen scenarios, reconcile requests/results, meet P95/error limits, persist every acknowledged message, retain complete 164-table database snapshots and add no dead Outbox event. Malformed nested arrays return controlled validation failures rather than crashing the verifier.
- Every remaining controlled suite now rejects shallow PASS claims: RLS proves denied read/write with zero exposure or mutation, tenant context proves alternating scopes and reset, Inbox proves duplicate delivery with one result, intake proves clean scanning plus hash-bound retained originals and OCR provenance, commerce proves contention and ledger reconciliation, payments prove signed/replay-safe callbacks and query-before-retry refund convergence, WeChat proves official-tool build/publish/device/rollback identity, identity/privacy prove revocation/MFA/least-privilege/deletion, and GEO/runtime policy prove exact target publication with default-deny egress. These checks are local verifier guarantees only; the underlying real-provider and controlled-environment runs remain pending.
- GitHub Actions run `32039457455` passed clean PostgreSQL 15 schema application and RLS isolation for commit `8da963b`.
- GitHub Actions run `32040005789` passed the revenue-right and frozen-policy PostgreSQL constraint test for commit `cb98f7b`.
- GitHub Actions run `32040375789` passed exact distribution reconciliation and immutable-ledger tests for commit `b88903f`.
- GitHub Actions run `32040722489` passed the subscription cash-source ledger and incremental 73→74-table migrations for commit `8a03bda`.
- GitHub Actions run `32041550030` passed the statement-locking PostgreSQL fixture for commit `cee60da`, including exact allocations, one statement/event/audit set and identical idempotent replay.
- GitHub Actions run `32042648486` passed commit `b5b3612`: clean and incremental PostgreSQL 15 schemas, dual-control constraints, provider-evidenced payout, linked paid-statement reversal and identical command replays.
- GitHub Actions run `32047162811` passed commit `c8236a5`: clean and incremental 73→77-table PostgreSQL 15 schemas, intake safety/immutability guards, signed-role service flow, rejected-file blocking, conflict retention, explicit confirmation, formal commit and idempotent replays.
- GitHub Actions run `32055811314` passed commit `fd29d49`: all code-quality gates plus clean and incremental 73→78-table PostgreSQL 15 schemas, upload-ticket immutability, object-evidence completion, signed internal WeCom/text boundaries and the trusted merchant-intake integration replay.
- GitHub Actions run `32056896317` passed commit `8583984`: 61 unit tests, the live Web API client/build, all code-quality gates and both clean/incremental PostgreSQL contract jobs.
- GitHub Actions run `32057022551` passed commit `482ba9d`: the documented live-workbench checkpoint retained both code-quality and PostgreSQL contract gates.
- Payment sandbox, real WeCom/provider callbacks, physical/WAL cross-fault-domain recovery, controlled V5 zero-data inventory/greenfield approval and real WeChat acceptance remain unverified. Local encrypted logical recovery and expand-only compatibility are verified but do not replace those controlled results; any discovered V5 record restores the full migration rehearsal gate.

## Exact next step

After the development candidate is pushed, require the GitHub CI result for the exact commit. Then run authenticated PostgreSQL-backed acceptance for all platform-consumer Life normal states and design the explicit merchant-context session handoff needed by PAGE-254/255/258/262/264 before enabling their writes. Retain WeChat DevTools, real-device, payment/provider and controlled-environment acceptance as external work. Deploy the explicitly non-production `bao.lequ.com` preview only through the root Compose entrypoint; it must not receive real users, merchants, payments or provider credentials. Production activation remains closed until the 29 controlled cases, seven external gates and reviewed eleven-suite bundle are bound to the exact candidate and deployed image digests.
