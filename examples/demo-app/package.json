{
  "name": "demo-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint src --ext .js",
    "test": "node --test tests/*.test.js",
    "build": "node -e \"console.log('build ok')\""
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
