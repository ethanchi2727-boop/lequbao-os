# Stage 10 release-candidate evidence

Date: 2026-08-19

## Outcome

The local release candidate is structurally mapped, but production activation is **blocked**. The frozen inventory contains 143 acceptance cases and 197 executable leaves: 135 乐趣宝 Web leaves, 38 乐趣生活 mini-program leaves and 24 merchant mini-program leaves. Every case and all 197 leaves now have an authoritative repository implementation. This closes local product-surface scope only; the controlled-environment and external gates below still block production activation. See `LAUNCH_READINESS_AUDIT.md`.

## Local evidence

- The 乐趣宝 runtime resolves all 135 frozen leaf routes and preserves all eight contracted states. PAGE-014 and PAGE-175–178 retain their live intake implementation. The other 130 launch-scope pages now read authoritative employee APIs for identity context, organization/roles/audit/privacy/internal notifications, sales/quotes/contracts/collection, subscription activation/renewal/cancellation, bounded employee Agent conversations/tasks/approvals, tenant-timezone today operations/todos, scoped GEO health/differences and idempotent publication, usage/revenue/costs/rights/disputes, bounded cost evidence, intake and merchant-data conversations/evidence/confirmations, delivery projects/exceptions, mini-program authorization/configuration/review, customer-service queues/duty/tasks/quality/conversion, WeCom connection health, reward rules, official plugins and skills, platform merchant/plan/partner/model controls, pseudonymous customers and rewards, knowledge publications, monthly reports, merchant profile, products/group buys and publication, orders, refunds, verification uses and daily reconciliations.
- Production mode fails closed: no employee token exposes no business cards, missing authoritative resource IDs render an empty prerequisite state, and pages without an implementation-bounded API render as unavailable instead of simulated success. Contract-only data is shown only behind `demo=1` with a persistent “演示模式 · 非真实业务数据” label. The final reverse audit also removed sample employee/store/task/progress chrome from production, routed desktop/mobile navigation to real page contracts and prevents send/upload/confirm from synthesizing success without an authoritative intake service and session.
- Browser checks covered a generic report page and the intake flow at desktop and 390×844 mobile widths. Empty/error state copy is page-generic, the mobile layout has no horizontal overflow and the Harness task/result/source rail remains interactive.
- Browser re-acceptance on 2026-08-19 covered the new workbench command boundary. At desktop width an unauthenticated customer-service detail page displayed `当前身份无权访问`, rendered zero live records, exposed zero enabled footer actions and had no console warning/error. At a 390×844 override the delivery detail page retained the explicit `演示模式 · 非真实业务数据` label, exposed no enabled simulated action and had no horizontal overflow.
- A final production-path browser pass is recorded in `docs/acceptance/2026-08-19-production-fail-closed-browser.md`: non-demo desktop/mobile rendering contained no sample identity, merchant, location, progress, count or demo-only composer tools; every shell navigation target was real, widths did not overflow and the browser warning/error log remained empty.
- Final full-surface browser acceptance on 2026-08-19 covered PAGE-166 at 1280×720 and PAGE-069/PAGE-130 at 390×844. Missing APIs or resource IDs rendered truthful empty/prerequisite states, the model form exposed no secret field, empty required input did not submit, plugin detail exposed no install action without an exact plugin code, there was no horizontal overflow and the console had zero warnings/errors. See `../acceptance/2026-08-19-full-surface-browser.md`.
- Migration `0016` adds ordered consumer offsets and durable dead-letter evidence. Worker logic now publishes or retries claimed Outbox events, caps delivery at twelve attempts, detects aggregate-version gaps, deduplicates through Inbox receipts and explicitly replays the original event ID.
- The Worker contract now has real local PostgreSQL fault-injection evidence for transaction-local tenant reset, mismatch rejection, retry, Inbox deduplication, version-gap rollback, dead-lettering and original-event replay. CI runs the same integration; independent controlled review is still required.
- Deployment-entrypoint review found that the Worker previously claimed Outbox rows without publishing or settling them. It now fails before claiming when the event gateway is unconfigured, sends the complete tenant/event envelope with event-ID idempotency, classifies gateway failures without provider-body leakage, settles published/retry/dead outcomes and reclaims crash-stale locks after five minutes. Unit tests and the real PostgreSQL runtime integration pass.
- Portable production-only API and Worker packages now start independently: API liveness/readiness passed and a one-shot Worker published and settled 20/20 events with no row left `PROCESSING`. A non-root multi-target Dockerfile packages API, Worker and the repository-owned hardened Web server; GitHub CI builds all three targets and smokes the Web image. This workstation has no Docker-compatible runtime, so the Linux CI image build remains required before candidate freeze.
- PRI-001 now has a tenant-scoped Worker contract for KMS envelope encryption, a maximum fifteen-minute export, delivery only to the verified consumer session and immutable audit evidence. Invalid or unencrypted gateway receipts fail closed.
- `acceptance-evidence-map.json` accounts for all 143 original IDs. The local evidence gate reports 111 automated passes, 3 locally reviewed cases and 29 cases that require a controlled environment.
- `controlled-acceptance-plan.json` assigns each of those 29 cases exactly once to an executable suite and covers all seven external gates, including external-only WeChat and identity/secrets/privacy/on-call suites. Every suite names the responsible role, runbook, isolated artifact directory, required evidence and pass criteria. The verifier rejects omissions, duplicates, unsafe directories, missing runbooks or incomplete gate coverage; the plan does not mark any external result as passed.
- The audited target is 164 tables across twenty-six migrations. The implementation-bounded OpenAPI has 193 paths, including the production identity-provider exchange boundary.
- The latest full local gate passes formatting, lint, contract/RBAC/page/visual/OpenAPI/security/operations/deployment checks, all TypeScript checks, 86 test files with 435 tests (including the controlled preflight, protected GHCR publisher and digest-bound performance topology, read-only V5 inventory, v2 result provenance/assembly, development-only provider gateway, exact Workbench leaf coverage, Worker publisher, production UI policy, hardened runtime/identity configuration, multi-tenant WeCom resolver and production Web-server tests) and all six production builds. Commit `e89fb91` is pushed and its duplicate push/PR GitHub workflows pass code-quality, PostgreSQL and all three container builds; the newer development-mock isolation batch is locally green and awaiting its follow-up commit.
- API and Worker production images now refuse startup when any launch-flow provider is silently disabled. Non-production profiles may omit a whole provider group, but partial groups are rejected. CI builds both candidates and asserts this fail-closed behavior before Web smoke; `.env.example` and the deployment runbook enumerate the complete launch profile.
- Enterprise-WeCom callbacks now resolve CorpID and member identity through a credentialed multi-tenant HTTPS configuration gateway rather than a single environment-bound tenant/member. The adapter validates tenant/corp/member response schemas, rejects cross-Corp responses, permits only a trusted 404 as missing identity and enforces HTTPS plus a nontrivial service token.
- Active tenant reward rules are resolved and snapshotted into order items inside the inventory-locking order transaction. Payment callbacks grant only from that immutable snapshot and never join mutable product policy. Platform merchant search is limited to a security-definer database function that independently verifies an active platform membership and role and returns only the safe directory projection. Its PostgreSQL fixture now passes locally with both an unauthorized merchant-member rejection and an authorized platform-admin projection; formal controlled review remains pending.
- The performance gate cannot pass with a read-only smoke test. It requires core read, core write and customer-message persistence scenarios, rejects production-shaped targets, verifies every acknowledged customer-message ID in PostgreSQL, captures database size/row/transaction/block/temp/deadlock and Outbox backlog evidence before and after, and atomically creates a new JSON report without credentials. Real threshold results still require controlled pre-production.
- A local real-API rehearsal exposed and repaired two pre-production blockers: the employee context and consumer message scenarios now use separate least-privilege Bearer identities, and the PostgreSQL Outbox age snapshot uses valid aggregate filtering. A 5-concurrency/20-request-per-scenario run passed all frozen latency/error thresholds with 20/20 message IDs persisted; an intentionally missing database produced a redacted durable FAIL report. This is harness readiness, not production-shaped acceptance.
- The portable restore drill no longer stops after `pg_restore`. Its encrypted manifest includes a frozen per-tenant order/payment/refund/verification/reward digest; restore verifies the artifact hash and digest, requires observable privacy-deletion replay, runs every PostgreSQL fixture, enforces RPO ≤ 300 seconds and RTO ≤ 3,600 seconds, and writes a durable report. Real WAL/physical-backup, cross-fault-domain and production-size results remain controlled-environment evidence.
- A real local PostgreSQL 15.19 encrypted logical-backup drill passed on 2026-08-19 with RPO 97.01 seconds, RTO 15.04 seconds, matching ciphertext hash and financial digest, one privacy-deletion replay and 22/22 fixtures in a disposable verification database. This removes the local script/runtime blocker but does not claim the required physical/WAL, cross-fault-domain or independently reviewed production evidence.

## Production blockers

The following controlled or external evidence is still required before launch:

1. Independently reviewed, build-bound PostgreSQL evidence. Local PostgreSQL 15.19 clean/incremental migrations through `0026`, RLS, all database fixtures, inventory concurrency and Worker Outbox/Inbox fault injection now pass; the formal artifact set and independent review remain pending.
2. Independently reviewed inventory proving every formerly deployable V5 store contains zero production business records, plus the greenfield waiver. Any discovered record restores the original full migration-rehearsal gate.
3. Real WeChat merchant mini-program build, review, publish, callback and rollback sandbox evidence.
4. Real payment/refund sandbox results, including merchant-account reconciliation and unknown-refund query-before-retry.
5. Real GEO target publication and an isolated plugin runtime with enforced network egress policy.
6. Physical/WAL and cross-fault-domain backup recovery with production-sized data and independent review. The encrypted local logical restore, measured RPO/RTO, financial reconciliation and privacy replay already pass.
7. Production-shaped core API and customer-message load reports meeting the frozen P95/error thresholds.
8. Real identity provider, secret manager, object storage, privacy gateway and on-call alert delivery.

No frozen P0 leaf remains prototype-only or contract-only. This does not substitute local route coverage for controlled PostgreSQL, provider, WeChat, device or operational acceptance evidence.

All 62 mini-program store/catalog/discovery/cart/checkout/order/payment/address/invoice/aftercare/verification/reward/customer-service/privacy leaves now read authenticated server state. Static sample commerce results have been removed and demo-only state switching is explicitly labelled. Real device, location, WeChat and provider acceptance remain controlled-environment gates rather than local claims.

`pnpm launch:gate` requires `CONTROLLED_RESULTS_FILE` and `RELEASE_COMMIT`. It validates the exact candidate commit, current plan SHA-256, all 11 suite results, distinct executor/reviewer roles and opaque accountable subject IDs, ordered execution/review timestamps, the exact declared artifact set and each artifact SHA-256. The result schema is version 2; older role-only manifests fail. Without real controlled evidence it intentionally remains red; missing, extra, stale, mismatched or unreviewed evidence cannot turn it green.
