# 乐趣生活正式路由功能回退修复

## 范围

基于 `ea885916a48a2bd380f0bfcb79507e66bb2312dc`，恢复 PAGE-201、203、204、209、213 的真实发现、搜索、商品与团购页面，以及 PAGE-218、221、224、228、229、231、232、235、237、239、242、248、250、254、255、258、259、262、264 的权威业务组件入口。修复前，部分页面显示固定价格、商户、个人信息、概念文案或无效路由，且原有业务组件只被导入而未渲染。移除 `LifeJourneyPage.vue` 中误混入的非 CSS 文本。

## 契约与数据

- 影响用户：乐趣生活 H5 和微信小程序消费者。
- 页面和 API：上述页面恢复调用现有 `LifeJourneyPage`、`LifeServicePage`、`LifeSurface` 与 `lifeSession`，继续使用原有发现、购物车、核价、支付意图、订单、售后、授权和客服接口。
- 页面修复本身不改变数据表、事件、权限、资金规则或历史数据。客户端不把固定演示金额、授权状态或支付结果呈现为真实业务事实。同批结算抵扣安全迁移另见下文。
- 防回退：`frontend:check` 检查 38 个正式乐趣生活路由的概念稿及无效入口，并核对受影响页面确实渲染对应业务组件或数据页面。

## 验证与发布

先运行乐趣生活测试、结构检查、前端矩阵检查、300 项路由检查与 H5/微信双构建；再在有真实测试身份和 PostgreSQL 的环境验收搜索、商品、结算、授权与客服正常态及失败态。正式支付、微信真机、外部供应商和受控发布证据仍按现有发布门禁执行。发布时仅替换对应前端产物，回滚到上一个已验证的前端产物；没有数据库回滚或财务重算。财务对账以服务端原有订单、支付、退款和奖励台账为准。

## 同批本地门禁修复

- 远程基线有 70 个文件未符合仓库 Prettier 规则；执行统一格式化，未更改业务判断。修复格式化暴露的脆弱源码字符串断言，并让报价拒绝测试使用未来有效期，确保实际进入价格校验分支。
- 数据库权威 schema 已到 `0028`、165 张表，但开发容器与预览增量迁移停在 `0027`；补齐 `0028` 应用步骤，并同步部署、受控证据与性能快照的版本/表数契约。此处只验证了脚本和静态契约，尚未在 PostgreSQL 中实际执行迁移。
- 为已经记录 `0028` 的数据库新增 `0029`：抵扣记录必须匹配消费者账号、结算组、商户与订单，强制行级权限。新增 `0030`：从存量结算组回填消费者 ID，抵扣记录只能引用同一消费者的奖励。最新目标为 30 个迁移标记、165 张表；不重写订单、奖励金额或不可变台账。数据库约束遇到存量归属冲突时整笔迁移失败，应隔离调查，不能自动改写历史资金事实。
- 首次远程 PostgreSQL 初始化指出 `0028` 的复合外键缺少被引用列的唯一约束；在创建抵扣投影表前为 `reward_grants(tenant_id,id)` 增加唯一约束。该约束不改变原有主键或历史金额事实；远程 `postgres-contract` 已验证 clean schema。
- 全仓测试先构建其所需的 contracts、Harness adapter 和工具网关；工作台测试使用根工作区锁定的 Vitest；重新生成锁文件与 SBOM，使依赖安装和供应链校验使用同一份清单。
- 首次远程依赖审计发现传递依赖 `fast-uri` 与 `adm-zip` 的高危公告；将 `fast-uri` 3.x/4.x 分别锁到 3.1.6/4.1.3，`adm-zip` 锁到 0.6.1。重新安装、生成 SBOM 后，本地 `pnpm audit --prod --audit-level high` 通过（剩余 2 项低危、5 项中危）。
- 第二次远程门禁确认 PostgreSQL 契约与代码质量通过；容器镜像和两个开发栈因未先构建 Harness adapter/工具网关而无法解析 API 的工作区导入。把三个共享运行时包的构建放在容器通用源码阶段，并同步开发容器初始化脚本。第三次远程容器和两个开发栈通过。
- 结算服务为新抵扣记录写入消费者 ID；SQL 测试拒绝跨账号结算组和跨消费者奖励。CI 在 `0028` 数据库中先提交测试抵扣与奖励，再升级到 `0029/0030`，核对原记录和金额不变，并复跑隔离测试。`d06c370a1b2bf4de077a5f24e1a7338aadb6ed23` 的 PostgreSQL、开发栈、容器和代码质量检查全绿。该 fixture 不能替代真实备份/恢复或大库锁等待验收。
- 新增只读 `database/preflight/checkout-reward-upgrade.sql`：在静止写入的 `0028` 库上，以迁移身份汇总检查结算账号、结算组、奖励归属、消费者、订单与状态是否冲突；仅报告分类计数，不输出身份或资金明细。预览增量迁移先执行预检，CI 注入跨账号历史抵扣后要求预检失败，再恢复合法事实并升级。实际受控旧库仍需备份、静止写入、预检和人工调查冲突，不能按计数自动修复财务记录。
- 开发栈 HTTP 冒烟在提交订单后请求 `SANDBOX` 支付意图；因开发种子没有经确认的商户支付账户，必须收到 `INVALID_COMMERCE_STATE`，订单保持 `PENDING_PAYMENT`，PostgreSQL 中支付意图仍为零。该验证证明未经确认不能进入资金动作，不证明真实支付或回调。
- CI 对带有已提交抵扣记录的 `0028` 测试库执行 `pg_dump` 自定义格式备份，恢复到独立库，在恢复库上预检并升级到 `0030`，检查迁移标记、原抵扣/奖励金额和原库仍停在 `0028`；随后继续在原测试库执行冲突注入和增量升级。该演练只覆盖小规模合成数据，不能替代生产同等规模备份、真实恢复时长、锁等待或业务停写验证。
- 本机默认 Vitest 并发导致工具测试临时文件丢失和超时；限制为 4 个 worker 后，工具测试 194/194 通过，断言内容未减少。
- 首页存在永远不渲染的图片区块与仅为测试保留的图片导入，导致 H5 超出 8 MiB。移除虚假运行时引用，保留官方图片原件与哈希校验，修正测试检查真实页面和组件。H5 产物为 7,561,233 字节，低于 8,388,608 字节上限。

## 验证边界

当前远程提交 `d06c370a1b2bf4de077a5f24e1a7338aadb6ed23` 的代码质量、PostgreSQL 契约、容器构建、开发栈和 bao 预览栈五项检查均通过。本机最新工作树完整 `pnpm check` 通过：工具测试 194/194、API 测试 359/359，乐趣生活 74/74 测试及 H5、`mp-weixin` 构建通过；五类产物摘要为 `7426c266e4e96c0e`。开发栈以开发种子和真实 PostgreSQL 执行了登录、发现商品、加购、核价、提交订单、查到待支付订单的同源 HTTP 链路；它证明测试订单创建，不证明真实支付完成。

2026-09-25 只读取 GitHub `controlled-preproduction` 环境的配置名称：所需 30 个 Secret 中已有 10 个，所需 26 个变量中已有 2 个，名称类型无额外项；阶段 50 另需受控结果文件。该盘点没有读取 Secret 值，也没有验证变量内容、外部服务或审批人，因此受控预检尚不具备启动条件。

## 后续开工与验收顺序

| 优先级 | 工作和准入证据                                                                                                                                                          | 当前边界                                                                                   |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| P0     | 在隔离 PostgreSQL 15+ 上以生产同等规模的备份对已有 `0028` 库执行 `0029/0030`，复核 30 个迁移标记、165 张表、消费者/租户 RLS、锁等待、备份与恢复；保存执行回执和回滚预案 | CI 已验证带已提交抵扣记录的增量升级；真实备份恢复和规模效应未验收                          |
| P0     | 使用受控测试身份走乐趣生活真实商户商品、库存、结算、支付回调、退款/售后与奖励台账对账；核对订单/支付/退款状态和幂等重放                                                 | 开发栈只到待支付订单，模拟网关不能证明供应商资金闭环                                       |
| P0     | 在 `controlled-preproduction` 中补齐并独立核验阶段 47–50 的配置名称、值和批准身份，再运行受控预检                                                                       | 当前缺 20 个 Secret、24 个变量；另需阶段 50 的结果文件，名称存在也不代表值有效             |
| P1     | 微信真机验证 H5/小程序登录、授权撤销、弱网恢复、38 个正式页面和支付/核销；保存设备、版本、截图与失败态证据                                                              | `mp-weixin` 构建和路由测试已过，真机交互未验收                                             |
| P1     | 用供应商沙箱账号验证微信支付、短信/企微、对象存储及其他被业务流程引用的外部网关，确认回调签名、重试和对账文件                                                           | 当前没有对应沙箱凭据及可审计回执                                                           |
| P1     | 执行受控阶段 47–50 的浏览器、性能、隐私删除/导出、故障恢复、灰度与回滚验收；按页面完成表逐项附证据后再讨论上线                                                          | 当前 197/197 页面 `accepted=false`，其中乐趣生活 38/38；上线动作及资金相关配置仍需人工确认 |

本工作站没有 `psql` 或 Docker；上述受控环境、供应商和真机步骤需在具备相应资源的环境完成。发布前按 `docs/release/frontend-page-completion.json` 更新逐页验收状态，并复跑当前提交的全部门禁。历史订单、奖励和财务台账不作重写；回滚应用产物时保留不可变资金事实并做服务端对账。

迁移发布先备份和校验旧库，再按版本顺序执行；`0029/0030` 增加权限和归属约束，不以删除约束作为业务回滚。若迁移失败，事务回滚后保留旧库和错误证据；若应用需回退，恢复上个已验证应用产物，并继续按原始订单、奖励和台账核对。真实数据库恢复演练必须在受控环境完成。
