(()=>{const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)], body=document.body, toast=$('.toast');let timer;
function notice(text){toast.textContent=text;toast.classList.add('show');clearTimeout(timer);timer=setTimeout(()=>toast.classList.remove('show'),1800)}
function closePopovers(){ $$('.popover').forEach(x=>x.classList.remove('open')) }
function setMode(mode){body.dataset.mode=mode;$$('[data-screen]').forEach(x=>x.classList.toggle('active',x.dataset.screen===mode));$('[data-title]').textContent=mode==='empty'?'新对话':mode==='chat'?'启动材料整理':'本周经营数据管理层汇报';body.classList.toggle('panel-open',mode==='task');$('.task-panel').classList.toggle('open',mode==='task');closePopovers();notice(mode==='task'?'复杂任务已展开任务与成果面板':mode==='empty'?'已创建新对话':'已打开普通对话')}
function openPopover(name,anchor){closePopovers();const p=$(`[data-popover=${name}]`);if(!p)return;p.classList.add('open');if(name==='tools'&&anchor){const r=anchor.getBoundingClientRect();p.style.left=Math.max(18,Math.min(innerWidth-270,r.left))+'px'}}
$$('[data-mode-target]').forEach(x=>x.addEventListener('click',()=>setMode(x.dataset.modeTarget)));
$$('[data-action]').forEach(x=>x.addEventListener('click',e=>{e.stopPropagation();const a=x.dataset.action;
 if(a==='collapse')body.classList.toggle('sidebar-collapsed');else if(a==='mobile-sidebar')body.classList.toggle('mobile-sidebar');else if(a==='theme')body.classList.toggle('dark');else if(a==='profile')openPopover('profile',x);else if(a==='conversation-menu')openPopover('conversation',x);else if(['upload','web','skills','plugins','voice','add'].includes(a)){openPopover('tools',x);$('[data-popover-title]').textContent={upload:'文件与图片',web:'联网研究',skills:'预置技能',plugins:'业务插件',voice:'语音输入',add:'添加内容'}[a];if(a==='upload')$('[data-upload]').click()}else if(a==='close-panel'){body.classList.remove('panel-open');$('.task-panel').classList.remove('open')}else if(a==='search')notice('搜索面板：对话、文件、项目、人员与业务对象');else if(a==='settings')notice('打开个人设置、记忆、通知与安全');else if(a==='mode')notice('切换快速问答、深度执行或仅检索');else if(a==='confirm')notice('正式执行前仍会校验权限、对象与幂等键');else if(a==='stop')notice('已请求安全停止，保留当前检查点');else notice({copy:'已复制',regenerate:'将从当前消息重新执行',sources:'打开来源与时效',trajectory:'打开完整执行轨迹',preview:'打开可编辑预览',download:'准备下载成果',edit:'进入协作编辑',share:'打开分享设置',more:'更多：重命名、收藏、导出、归档',help:'打开帮助与反馈',feedback:'感谢反馈'}[a]||'该交互已纳入开发契约') }))
document.addEventListener('click',closePopovers);document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='n'){e.preventDefault();setMode('empty')}if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();notice('搜索面板已打开')}if(e.key==='Escape'){closePopovers();body.classList.remove('mobile-sidebar')}});$('[data-composer]').addEventListener('submit',e=>{e.preventDefault();setMode('chat')});setMode(new URLSearchParams(location.search).get('mode')||'empty');if(new URLSearchParams(location.search).get('theme')==='dark')body.classList.add('dark')})();
